{
  "name": "s3-file-upload-system",
  "version": "1.0.0",
  "description": "S3 File Upload System with support for large files (1GB+)",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev -w @s3-upload/server",
    "dev:client": "npm run dev -w @s3-upload/client",
    "build": "npm run build -w @s3-upload/server && npm run build -w @s3-upload/client",
    "start": "npm run start -w @s3-upload/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
